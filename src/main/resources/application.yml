server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/IvanAlejandroMancilla/tp-config-repo-IVANMANCILLA
          default-label: main

